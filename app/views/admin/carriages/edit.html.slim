h3
  | Editing Carriage
= render 'form', carriage: @carriage
= link_to 'Show', [:admin, @carriage], class: 'btn btn-default'
= link_to 'Back', admin_train_path(@carriage.train), class: 'btn btn-default'
