h1.title
  = t('.title')
= render 'form', train: @train
.section
= link_to t('common.show'), [:admin, @train], class: 'button is-warning'
|    
= link_to t('common.back'), admin_trains_path, class: 'button is-primary'
